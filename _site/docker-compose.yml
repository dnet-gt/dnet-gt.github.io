services:
  jekyll:
    image: ruby:3.2
    working_dir: /site
    ports:
      - "4000:4000"
    volumes:
      - .:/site
    command: >
      bash -c "
      bundle install &&
      bundle exec jekyll serve --host 0.0.0.0 --livereload
      "
